<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://zacsmms.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://zacsmms.github.io/" rel="alternate" type="text/html" /><updated>2026-09-07T01:52:01+00:00</updated><id>https://zacsmms.github.io/feed.xml</id><title type="html">Zachary Scott-Murphy</title><subtitle>Writing on computer vision, machine learning, and the mathematics underneath them.</subtitle><author><name>Zachary Scott-Murphy</name></author><entry><title type="html">Least Squares from 4 Angles</title><link href="https://zacsmms.github.io/writing/least-squares/" rel="alternate" type="text/html" title="Least Squares from 4 Angles" /><published>2026-05-15T00:00:00+00:00</published><updated>2026-05-15T00:00:00+00:00</updated><id>https://zacsmms.github.io/writing/least-squares</id><content type="html" xml:base="https://zacsmms.github.io/writing/least-squares/"><![CDATA[<p>This topic was not one I expected to misunderstand so much. For a topic in Machine Learning, Least Squares is one of the fundamentals by far. However, it has more nuance and interoperable techniques that I’ve touched upon prior.</p>

<p>For this post, I’ll approach Least Squares from 1 angle, to do that we need to explore the Moore-Penrose Pseudoinverse.</p>

<p>The motivation for the Pseudoinverse is best understood geometrically before tackling the mathematics. Consider first a matrix A:</p>

\[A = \begin{pmatrix} 1 &amp; 3 \\ 2 &amp; 0 \\ 3 &amp; 0 \end{pmatrix}.\]

<p>This is a rectangular matrix that has column vectors \(v_1 \text{ and } v_2\) which define the column space (space defined by any linear combination of them). Defining the linear combination of them in this way we can understand the column space as a plane containing the region describable with them. The resulting graph looks as such!</p>

<figure>
  <img src="/assets/img/column_space.png" alt="Column space of two vectors creating a plane described in grey." loading="lazy" />
  <figcaption>Column space of two vectors creating a plane described in grey.</figcaption>
</figure>

<p>This is a unitary portion of the overall column space for simplicity, but it spans in all directions achievable by their linear combination. Feel free to play around with this yourself. Remember that this describes the column space because it is the vectors defined by the columns of matrix A.</p>

<p>Now, any point defined in the column space is easily projected with the pseudoinverse of matrix A back to where it would have lied on the 3D graph axes known so well! As seen in the image prior, I’ve defined a point in that space right at the centre of the column space of a half combination of both column vectors. The pseudoinverse of A is non-trivial but it is,</p>

\[A^{+} = \begin{pmatrix} 5.61218077\text{e-}17 &amp; 1.53846154\text{e-}01 &amp; 2.30769231\text{e-}01 \\ 3.33333333\text{e-}01 &amp; -5.12820513\text{e-}02 &amp; -7.69230769\text{e-}02 \end{pmatrix},\]

<p>Not exactly the prettiest, but it does result in a known location for the half point in A’s column space, directly at point (0.5, 0.5) luckily (or mayhaps not luck)!</p>

<p>The point of contention here is that an inverse for this case was only easy because the point was described in a square portion of the matrix that we needed to consider. Non-square inverse matrices are not exactly a real concept, so we fill in the gap by introducing the Moore-Penrose Pseudoinverse, which happens to be exactly what was used to produce the non-square inverse matrix seen prior.</p>

<p>To continue on the geometric understanding of the motivation, we were lucky enough to have a point described in the column space which consists of exactly 0 elements defined outside of a square portion of that matrix. If we had a point slightly above the surface, without the pseudoinverse it would be impossible to generate a description of it in the column-space of A. And that is effectively all but 2-dimensions accounted for by A, so there’s a lot of space missing and lots of possible points that we miss! To visualise this, here is a 2D example of two points, one lying on the surface, another lying outside the surface.</p>

<figure>
  <img src="/assets/img/basis_column.png" alt="Basis space and column space representation of two points, one lying in the column space and one lying outside of it." loading="lazy" />
  <figcaption>Basis space and column space representation of two points, one lying in the column space and one lying outside of it.</figcaption>
</figure>

<p>Here, there are two lines defining a 1-dimensional space where 2-dimensions can be described. The green point lies in the plane and is easily defined by the square components of the matrix. The red point lies outside the plane and relies on a column space description that extends to the second dimension, so an additional matrix component needs to define the value. The red point respectively gets projected into column space roughly above the centre between the green point and the end of the blue vector.</p>

<p>Now for the mathematical intuition, consider a rectangular matrix A and its SVD,</p>

\[A = U \Sigma V^T,\]

<p>as we showed in the previous post, the matrices U and V are square individually, but not in combination. The property that we want the pseudoinverse to hold is this,</p>

\[A^+ = V \Sigma^+ U^T,\]

<p>now is where we consider a goal to help us approach a definition, a sort of definition by characterisation. The goal is to find the pseudoinverse of course, but why we’re here in the first place is to understand Least Squares! That is minimising x for,</p>

\[||Ax - b||,\]

<p>where A is our matrix that is not of equal size, b is some point in basis space, and x is the value we’re looking to minimise. We start by defining two terms,</p>

\[c = U^T b,\]

\[z = V^T x.\]

<p>V forms an orthonormal set so it follows that,</p>

\[x = Vz \rightarrow ||x|| = ||z||,\]

<p>therefore,</p>

\[Ax = U\Sigma z,\]

<p>and</p>

\[||Ax - b|| = ||\Sigma z - c||.\]

<p>Which reduces the problem to a diagonal problem, because \(\Sigma\) is diagonal. A search over a diagonal only cares about the rank r, resulting in,</p>

\[||\Sigma z - c||^2 = \sum_{i=1}^r (\sigma_i z_i - c_i)^2 + \sum_{i=r+1}^M c_i^2.\]

<p>Now because we’re minimising a Least Squares problem, this results in the term by term component,</p>

\[\sigma_i z_i - c_i = 0 \rightarrow z_i = \frac{c_i}{\sigma_i}.\]

<p>Despite this solution for z being optimal, we have too many options of the fraction definition. To pick the best one, we need to pick the point of the column space closest to the origin, which looks like,</p>

\[||z||^2 = \sum_{i\leq r} z_i^2 + \sum_{i &gt; r} z_i^2.\]

<p>Which drives each component to 0, for every \(i &gt; r\). What this means is that we define every component up to the base of the column space, after which nothing! In other words, this describes \(\Sigma \in \mathbb{R}^{n \times m}\), so x becomes,</p>

\[x = V \Sigma^+ U^T b.\]

<p>Which holds for every b so the operator that produces the best x is,</p>

\[A^+ = V \Sigma^+ U^T.\]

<p>What an interesting result! Randy Pausch would be proud of showing you both head fakes and actually going both directions at the same time. What we’ve done here has been coming in with the goal of understanding the Least Squares, but needing to define the Moore-Penrose pseudoinverse and using both as stepping stones for each other to solve each; despite the pseudoinverse being naturally harder to derive from first principles.</p>

<h1 id="conclusion">Conclusion</h1>

<p>This is probably the best approach you can take to understand both topics at once. I highly recommend readers derive from first principles, the Least Squares scenario via algebra, geometry, SVD, and probability. If you need hints, the topics of my two prior posts should give you enough headway. Also if you want some spare reading, that little mention I put in of Prof. Pausch is a great place to look, he felt like a modern-day Feynman! But maybe that is just a hint that most professors in our field have lots to say, just only a few have or had a platform to share it. Regardless, check out The Last Lecture!</p>

<p>And here’s a boring Least Squares graph, because there can only be space for one cool topic like the pseudoinverse!</p>
<figure>
  <img src="/assets/img/least_squares.png" alt="Boring Least Squares problem..." loading="lazy" />
  <figcaption>Boring Least Squares problem...</figcaption>
</figure>]]></content><author><name>Zachary Scott-Murphy</name></author><category term="linear-algebra" /><category term="fundamentals" /><summary type="html"><![CDATA[Covering Least Squares solutions from the goal of describing the Moore-Penrose Pseudoinverse.]]></summary></entry><entry><title type="html">Singular Value Decomposition &amp;amp; A Coda for Principal Component Analysis</title><link href="https://zacsmms.github.io/writing/svd-pca/" rel="alternate" type="text/html" title="Singular Value Decomposition &amp;amp; A Coda for Principal Component Analysis" /><published>2026-05-13T00:00:00+00:00</published><updated>2026-05-13T00:00:00+00:00</updated><id>https://zacsmms.github.io/writing/svd-pca</id><content type="html" xml:base="https://zacsmms.github.io/writing/svd-pca/"><![CDATA[<p>The connecting component between Singular Value Decomposition (SVD) and Principal Component Analysis (PCA) is the eigendecomposition. Principal component analysis requires keeping the top-k eigenvectors for reconstruction of the maximal information components. We can study the effect of this with a Gaussian.</p>

<p>Following on from the previous post, we used a covariance matrix to define conditionally dependent multivariate Gaussians. The covariance matrix can be thought of as a set of operations, a rotation, followed by scaling, finishing off with a final rotation. This looks like,</p>

\[X = U \Sigma V^T,\]

<p>for a given matrix X, the rotation components \(U\) and \(V\), with scaling \(\Sigma\). To relate to eigendecomposition, we consider a symmetric Positive SemiDefinite (PSD) matrix M,</p>

\[M = X^T X,\]

<p>where X is not necessarily square, but M is. Substituting in the SVD for X and eliminating variables results in,</p>

\[X^T X = V \Sigma^2 V^T,\]

\[X X^T = U \Sigma^2 U^T.\]

<p>These equations are eigendecompositions themselves for the left and right eigenvectors, so the singular values of X are,</p>

\[\sigma_i = \sqrt{\lambda_i(X^T X)},\]

<p>or the square roots of the non-zero eigenvalues of \(X^T X\) (equivalently \(XX^T\)). Therefore, the columns of \(U\) are eigenvectors of \(XX^T\) and the columns of \(V\) are eigenvectors of \(X^T X\).</p>

<p>The link between SVD and eigendecomposition allows PCA to perform a low-rank execution of decomposition for simpler operations. By taking the high dimensional dataset that PCA receives, and reducing to the axes with maximal variance, we retain the most information. With Gaussian covariance descriptions, the eigenvectors corresponded to ellipsoidal directions. Meaning that we collapse the meaningful information because it is easy to reproduce and it corresponds to a majority of the overall feature.</p>

<figure>
  <img src="/assets/img/principal_components.png" alt="Principal components of a 3D Gaussian; vectors PC1, PC2, and PC3" loading="lazy" />
  <figcaption>Principal components of a 3D Gaussian; vectors PC1, PC2, and PC3</figcaption>
</figure>

<h2 id="low-rank-and-truncated-svd">Low-Rank and Truncated SVD</h2>

<p>To avoid hand-waving completely, I’ll revisit the low-rank concept for SVD. To begin, the reduced SVD assumes that for any SVD there are columns in U whose corresponding singular values are zero, so they contribute nothing to the reconstruction. Specifically for \(A \in \mathbb{R}^{m\times n}\) and \(m \ge n\),</p>

\[U \in \mathbb{R}^{m \times m}, \Sigma \in \mathbb{R}^{m \times n}, V \in \mathbb{R}^{n \times n},\]

<p>we can define the reduced SVD as,</p>

\[\tilde{U} \in \mathbb{R}^{m \times n}, \tilde{\Sigma} \in \mathbb{R}^{n \times n}, \tilde{V} \in \mathbb{R}^{n \times n}.\]

<p>This reduces the number of operations total that we need to perform. The rank of the matrix is then defined as the number of non-zero diagonal elements in \(\Sigma\). This brings another question in mind. Because there are certain columns of the matrices that we don’t need, can we take only the top-k elements from the decomposition for reconstruction?</p>

<figure>
  <img src="/assets/img/truncated.png" alt="Top-2 Elements from Truncated SVD and Reconstruction; grey projects shows that the recovered gaussian is in 2D" loading="lazy" />
  <figcaption>Top-2 Elements from Truncated SVD and Reconstruction; grey projects shows that the recovered gaussian is in 2D</figcaption>
</figure>

<p>To the machine learner, this is obviously PCA, and it directly follows that taking the top-k is an information preserving operation. But why is this?
The matrix we consider is the eigendecomposition for simplicity, the diagonal elements are the eigenvalues and we get their retrieved eigenvectors from the other matrices.
By considering a low-rank representation of the decomposition, we are taking only the first few eigenvalues, which means that the eigenvectors along other dimensions are zero-ed out.
We order the eigenvalues based on maximal variance so that they describe the largest change.
This top-k component procedure we perform to retain the eigenvectors which describe the principal information, by ordering based on variance.
Therefore, PCA takes the decomposition to reduce the amount of information in a feature dataset, by describing maximal variance in the eigendecomposition.</p>

<h1 id="conclusion">Conclusion</h1>

<p>We found out here exactly why PCA is related to eigendecomposition by the connection of eigendecomposition to SVD. PCA uses the low-rank description of a decomposition to retain maximally varying information in the eigenbasis. That’s the sum of everything we did here, just in a shorter form!</p>]]></content><author><name>Zachary Scott-Murphy</name></author><category term="linear-algebra" /><category term="fundamentals" /><summary type="html"><![CDATA[Deriving the relationship between SVD to eigendecomposition and their relations to PCA.]]></summary></entry><entry><title type="html">Multivariate Gaussian PDFs</title><link href="https://zacsmms.github.io/writing/multivariate-gaussians/" rel="alternate" type="text/html" title="Multivariate Gaussian PDFs" /><published>2026-05-12T00:00:00+00:00</published><updated>2026-05-12T00:00:00+00:00</updated><id>https://zacsmms.github.io/writing/multivariate-gaussians</id><content type="html" xml:base="https://zacsmms.github.io/writing/multivariate-gaussians/"><![CDATA[<p>Gaussian probability density functions (PDFs) provide a new domain to understand 3-dimensional information using Gaussian Splatting (Kerbl et al., 2023). This technique, influenced by Neural Radiance Fields (Mildenhall et al., 2020), opened up deep learning techniques without the high compute barrier of the neural network representation.</p>

<p>Here I derive the basic properties of multivariate gaussians from fundamentals.</p>

<h2 id="1d-gaussian-pdf">1D Gaussian PDF</h2>

<p>The 1D Gaussian PDF is of the form,</p>

\[p(x) = \frac{1}{\sqrt{2 \pi \sigma^2}} \exp\left( -\frac{(x - \mu)^2}{2 \sigma^2} \right),\]

<p>where \(\sigma\) is the standard deviation (\(\sigma^2\) the variance), controlling the spread of the gaussian simultaneously with the peak height, and \(\mu\) is the mean. This equation is alternatively a normal distribution and describes a standard probability distribution found across a variety of statistical scenarios.</p>

<h2 id="2d-gaussian-pdf-for-independent-variables">2D Gaussian PDF for Independent Variables</h2>

<p>A logical next step is how to define the Gaussian PDF in 2D; if we can define a Gaussian PDF in 2D then we can define an N-dimensional Gaussian PDF. We start off from the case of independent variables, which is just a resulting product of Gaussian PDFs,</p>

\[p(x_1, x_2) = p(x_1) \cdot p(x_2) = \frac{1}{2 \pi \sigma_1 \sigma_2} \exp\left( -\frac{(x_1 - \mu_1)^2}{2 \sigma_1^2} - \frac{(x_2 - \mu_2)^2}{2 \sigma_2^2} \right),\]

<p>with two independent variances and means. The resulting Gaussian PDF is allegorically a series of concentric ellipses with increasing distance between them. Then, let \(x = [x_1, x_2]\) and \(\mu = [\mu_1, \mu_2]\) and we rewrite the equation,</p>

\[p(x_1, x_2) = \frac{1}{2 \pi |A|^{1/2}} \exp\left( -\frac{(x - \mu)^T A^{-1} (x - \mu)}{2} \right),\]

<p>where</p>

\[A = \begin{pmatrix} \sigma_1^2 &amp; 0 \\ 0 &amp; \sigma_2^2 \end{pmatrix}\]

<p>is the covariance matrix in this independent case (note \(\lvert A \rvert^{1/2} = \sigma_1 \sigma_2\), so the prefactor agrees with the product form above). Its diagonal entries scale the two axes independently, so the level sets of \(p\) are axis-aligned ellipses — circles when \(\sigma_1 = \sigma_2\).</p>

<figure>
  <img src="/assets/img/multivariate_gaussian_independent.png" alt="2D Gaussian sample with zero correlation; 1σ, 2σ, 3σ level sets are axis-aligned ellipses." loading="lazy" />
  <figcaption>2D Gaussian sample with zero correlation; 1σ, 2σ, 3σ level sets are axis-aligned ellipses.</figcaption>
</figure>

<h2 id="2d-gaussian-pdf-for-dependent-variables">2D Gaussian PDF for Dependent Variables</h2>

<p>Here we consider the exponent; its form is</p>

\[-\frac{(x - \mu)^T A^{-1} (x - \mu)}{2}.\]

<p>The missing piece of \(A\) is the covariance between the variables. We extend the matrix to include those off-diagonal dependence components. The covariance between \(x_1, x_2\) is</p>

\[\text{cov}[x_1, x_2] = \mathbb{E}[(x_1 - \mu_1)(x_2 - \mu_2)],\]

<p>the expectation of the product between the centred variable components. The covariance matrix can therefore be defined as,</p>

\[\Sigma = \begin{pmatrix} \text{var}(x_1) &amp; \text{cov}(x_1, x_2) \\ \text{cov}(x_2, x_1) &amp; \text{var}(x_2) \end{pmatrix},\]

<p>where the off-diagonal entries \(\text{cov}(x_1, x_2) = \text{cov}(x_2, x_1)\) are equal because covariance is symmetric by definition. The resulting PDF is,</p>

\[p(x_1, x_2) = \frac{1}{2 \pi |\Sigma|^{1/2}} \exp\left( -\frac{(x - \mu)^T \Sigma^{-1} (x - \mu)}{2} \right).\]

<p>Geometrically, the off-diagonal terms rotate the level sets away from the coordinate axes — the ellipses tilt in the direction of correlation.</p>

<figure>
  <img src="/assets/img/multivariate_gaussian_dependent.png" alt="Same sample re-shaped with correlation rho = 0.5; the level-set ellipses now tilt along the principal axes of Sigma." loading="lazy" />
  <figcaption>Same sample re-shaped with correlation \(\rho = 0.5\); the level-set ellipses now tilt along the principal axes of \(\Sigma\).</figcaption>
</figure>

<h2 id="nd-gaussian-pdf-for-dependent-variables">ND Gaussian PDF for Dependent Variables</h2>

<p>The generalisation to \(N\) variables follows the same pattern. \(\Sigma \in \mathbb{R}^{2 \times 2} \rightarrow \mathbb{R}^{N \times N}\) keeps variances along the diagonal and fills the off-diagonals with covariances; the vectors \(x, \mu \in \mathbb{R}^2 \rightarrow \mathbb{R}^N\) simply grow in length. In the independent case the prefactor is a product of 1D normalisers,</p>

\[\prod_{i=1}^N \frac{1}{\sqrt{2 \pi \sigma_i^2}} = \frac{1}{(2 \pi)^{N/2} \prod_i \sigma_i},\]

<p>which generalises to \(1 / ((2 \pi)^{N/2} \lvert \Sigma \rvert^{1/2})\) in the dependent case (note \(\lvert \Sigma \rvert = \prod_i \sigma_i^2\) when \(\Sigma\) is diagonal, recovering the product form). The resulting equation is</p>

\[p(x_1, x_2, \ldots, x_N) = \frac{1}{(2 \pi)^{N/2} |\Sigma|^{1/2}} \exp\left( -\frac{(x - \mu)^T \Sigma^{-1} (x - \mu)}{2} \right).\]

<h2 id="marginals-of-joint-gaussian-distributions">Marginals of Joint Gaussian Distributions</h2>

<p>We now move on with deriving conditional probabilities.</p>

<h3 id="2d-independent-variables">2D Independent Variables</h3>

<p>This is the simplest case, solved logically or mathematically. The equation for the gaussian is a density function, so we can integrate over one variable,</p>

\[p(x_1) = \int_{-\infty}^{\infty} p(x_1, x_2) \,dx_2 = \int_{-\infty}^{\infty} p(x_1) p(x_2) \,dx_2 = p(x_1) \int_{-\infty}^{\infty} p(x_2) \,dx_2,\]

<p>where the factorisation is allowed because the PDFs are independent. The result is just \(p(x_1)\) because the area under a gaussian PDF is 1 (the total probability). Alternatively, you can notice that the marginal, if not conditioned on any other variable, results in an independent PDF.</p>

<h3 id="2d-dependent-variables">2D Dependent Variables</h3>

<p>For the dependent case, we consider the equation from before,</p>

\[p(x_1, x_2) = \frac{1}{2 \pi |\Sigma|^{1/2}} \exp\left( -\frac{(x - \mu)^T \Sigma^{-1} (x - \mu)}{2} \right).\]

<p>The conditional probability can be found from</p>

\[p(x_2 | x_1) = \frac{p(x_1, x_2)}{p(x_1)},\]

<p>where the denominator is the familiar 1D Gaussian PDF. Let \(\rho = \text{cov}(x_1, x_2) / (\sigma_1 \sigma_2)\) be the correlation, so that \(\lvert \Sigma \rvert = \sigma_1^2 \sigma_2^2 (1 - \rho^2)\) and the joint quadratic form expands as</p>

\[(x - \mu)^T \Sigma^{-1} (x - \mu) = \frac{1}{1 - \rho^2}\left[\frac{(x_1 - \mu_1)^2}{\sigma_1^2} - \frac{2 \rho (x_1 - \mu_1)(x_2 - \mu_2)}{\sigma_1 \sigma_2} + \frac{(x_2 - \mu_2)^2}{\sigma_2^2}\right].\]

<p>Completing the square in \(x_2\) separates it into a piece depending on \(x_2\) and one that does not,</p>

\[(x - \mu)^T \Sigma^{-1} (x - \mu) = \frac{[(x_2 - \mu_2) - \rho (\sigma_2 / \sigma_1)(x_1 - \mu_1)]^2}{\sigma_2^2 (1 - \rho^2)} + \frac{(x_1 - \mu_1)^2}{\sigma_1^2}.\]

<p>The second term, together with the matching factor in the joint’s normalisation, is exactly \(p(x_1)\), which cancels in the ratio. What remains is itself a 1D Gaussian in \(x_2\),</p>

\[p(x_2 | x_1) = \frac{1}{\sqrt{2 \pi \sigma_2^2 (1 - \rho^2)}} \exp\left( -\frac{(x_2 - \mu_{2|1})^2}{2 \sigma_{2|1}^2} \right),\]

<p>with conditional mean and variance</p>

\[\mu_{2|1} = \mu_2 + \rho \frac{\sigma_2}{\sigma_1}(x_1 - \mu_1), \qquad \sigma_{2|1}^2 = \sigma_2^2 (1 - \rho^2).\]

<p>So observing \(x_1\) shifts the mean of \(x_2\) along the regression line and shrinks its variance by a factor of \((1 - \rho^2)\). In the general \(N\)-D case the same algebra (block-matrix inversion) gives</p>

\[p(x_b | x_a) = \mathcal{N}\bigl(\mu_b + \Sigma_{ba} \Sigma_{aa}^{-1}(x_a - \mu_a),\; \Sigma_{bb} - \Sigma_{ba} \Sigma_{aa}^{-1} \Sigma_{ab}\bigr).\]

<figure>
  <img src="/assets/img/multivariate_gaussian_conditional.png" alt="Conditioning on y = 3 slices the joint cloud (top); the resulting 1D conditional density p(x mid y) has mean shifted along the regression line and variance shrunk by (1 - rho^2) (bottom)." loading="lazy" />
  <figcaption>Conditioning on \(y = 3\) slices the joint cloud (top); the resulting 1D conditional density \(p(x \mid y)\) has mean shifted along the regression line and variance shrunk by \((1 - \rho^2)\) (bottom).</figcaption>
</figure>

<h2 id="conclusion">Conclusion</h2>

<p>This concludes the overview on multivariate Gaussian PDFs, there’s many applications of Gaussian’s so understanding their basics is very important. If you are reading this and on your path to begin your academic career, I highly recommend deriving this yourself and coding up visualisations to help you understand the way the components interact.</p>]]></content><author><name>Zachary Scott-Murphy</name></author><category term="probability" /><category term="fundamentals" /><summary type="html"><![CDATA[Deriving multivariate Gaussian PDFs from the 1D case, with marginals and conditionals.]]></summary></entry></feed>